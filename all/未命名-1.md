```mermaid
flowchart TD
  %% 阶段顺序
  理论目标 --> 数据集构建
  数据集构建 --> 模型设计优化
  模型设计优化 --> 动态后处理
  动态后处理 --> 系统集成实现
  系统集成实现 --> 验证测试
  验证测试 --> 应用部署

  %% 各阶段内容
  subgraph 理论目标
    A1[突破传统FBG解调算法瓶颈]
    A2[构建融合深度学习的智能解调体系]
  end

  subgraph 数据集构建
    B1[参数化光谱模拟生成]
    B2[控制峰数(0–20)、峰宽(0.1–0.3 nm)、噪声(SNR 5–30 dB)、基线漂移]
  end

  subgraph 模型设计优化
    C1[轻量化1D-CNN五层主干网络]
    C2[双分支预测头(热力图 + 偏移量)]
    C3[多任务损失(热力图 / 偏移量 / 峰数)]
  end

  subgraph 动态后处理
    D1[基于预测峰宽的阈值调整]
    D2[多级置信度过滤(振幅 + 响应强度)]
  end

  subgraph 系统集成实现
    E1[标准化预处理(基线校正 / 归一化 / 校准)]
    E2[特征提取与模型推理]
    E3[后处理输出]
  end

  subgraph 验证测试
    F1[模拟数据验证(密集峰群 0.2 nm间距)]
    F2[真实数据测试(温度 / 应变场景)]
  end

  subgraph 应用部署
    G[智慧安防 & 智慧城市大规模部署]
  end

```